#include<iostream>

using namespace std;

int main(){
    int a, b,c,d;

    cout << "int a, b, c and d are: " << endl;

    cin >> a;
    cin >> b;
    cin >> c;
    cin >> d;

    cout<< (a>b && c>d);
    cout<< (a>b || c>d);

return 0;
}
